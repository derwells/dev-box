# Claude adapter
- Personal slash commands load through the Skill tool, for example `skill: "learn"`.
- Keep the existing Claude GSD installation, hooks, status line, and settings as the Claude runtime configuration.
